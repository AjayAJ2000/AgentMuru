## Summary

- What problem does this change solve?
- What changed in runtime, Workspace, provider, or Labs behavior?

## Validation

- [ ] `python -m pytest -q`
- [ ] `python -m ruff check agentmuru tests qualification examples`
- [ ] `python -m mypy agentmuru`
- [ ] `npm test -- --run && npm run lint && npm run typecheck && npm run build`
- [ ] `python -m mkdocs build --strict`
- [ ] `python -m build`

## Docs And Examples

- [ ] I updated the public API, provider, operations, or Labs docs when their contracts changed
- [ ] I updated examples if runtime behavior changed
- [ ] I updated root markdown files if install, release, or support guidance changed

## Risk Notes

- Known tradeoffs:
- Areas that deserve extra reviewer attention:
